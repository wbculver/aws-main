import boto3

def lambda_handler(event, context):
    bucket_name = 'bucketcflambda23'
    
    # Create an S3 client
    s3_client = boto3.client('s3')
    
    # Create the bucket
    response = s3_client.create_bucket(
        Bucket=bucket_name
    )
    
    return response